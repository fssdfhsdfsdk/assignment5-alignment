from setuptools import setup, find_packages

setup(
    name="hello_world_myself_test_figerzeta", # 包在 PyPI 上的名称，必须唯一
    version="0.1",
    packages=find_packages(),
    description="A simple hello world test package",
    author="figerZeta",
)